<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "we.are.Mapper.InventoryMapper">

	<insert id = "p_insert">
		insert into product(pcode,pname,pprice,pmc,pstock,pmi) 
		values(#{pcode},#{pname},#{pprice},#{pmc},#{pstock},#{pmi});
	</insert>

	<select id = "p_list" resultType = "we.are.Model.InventoryDTO">
		select * from product;
	</select>
	
	<select id = "p_detail" resultType = "we.are.Model.InventoryDTO">
		select * from product where pcode = ${pcode};
	</select>
</mapper>